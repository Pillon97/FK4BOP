{
  "name": "omega-feladatkezelo",
  "version": "1.0.0",
  "private": true,
  "description": "Hallgatói feladatkezelő — F# + Fable + Elmish SPA",
  "scripts": {
    "start": "dotnet fable watch src --run vite",
    "build": "dotnet fable src && vite build",
    "preview": "vite preview",
    "clean": "dotnet fable clean --yes src"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
